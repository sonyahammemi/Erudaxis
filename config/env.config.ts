/*export const ENV = {
    BASE_URL: process.env.BASE_URL || 'https://staging.erudaxis.com'
};*/

// tests/configEnv/env.config.ts
/*import dotenv from 'dotenv';
dotenv.config(); // ← charger ici directement

export const ENV = {
    BASE_URL: process.env.BASE_URL || 'https://staging.erudaxis.com'
};*/

export const ENV = {
    BASE_URL: process.env.BASE_URL || ''
};